# BlindingEntropy

:::info[Mirrored page]
Source: [`lean/docs-proofs/blindingentropy.md`](https://github.com/Skanislav/pq-sap/blob/main/lean/docs-proofs/blindingentropy.md). Edit the source file; this copy is regenerated by `pnpm sync` in `wiki/`.
:::

# Point-mass bound for the Construction A address point

## `blindPointSample`

```lean
def blindPointSample (P : Prims R Rho WireBytes T1 Tag Addr K k l)
    [SampleableType R] (rho : Rho) (t : Fin k → R) : ProbComp WireBytes :=
  do let u ← $ᵗ (Fin k → R)
     pure (P.pack rho (P.power2Round (u + t)))

```

[`BlindingEntropy.lean:15-19 ↗`](https://github.com/Skanislav/pq-sap/blob/main/lean/PqStealth/BlindingEntropy.lean#L15-L19)

## `blindBadProb_le_queryBound`

```lean
theorem blindBadProb_le_queryBound
    (P : Prims R Rho WireBytes T1 Tag Addr K k l)
    [SampleableType R] [DecidableEq WireBytes]
    (tg : Tag) (rho : Bool → Rho) (t : Bool → Fin k → R)
    (adv : BlindAdvRO Tag Addr WireBytes) (qH : ℕ)
    (beta : ℝ≥0∞) (hB : BlindPointMassBound P beta) (b : Bool) :
    blindBadProb P tg rho t adv b ≤ ((qH : ℝ≥0∞) * beta).toReal := by
```

:::details[proof]

```lean
  sorry
```

:::

[`BlindingEntropy.lean:25-33 ↗`](https://github.com/Skanislav/pq-sap/blob/main/lean/PqStealth/BlindingEntropy.lean#L25-L33)

## `mlDsaAddressPointBeta`

```lean
noncomputable def mlDsaAddressPointBeta (k : ℕ) : ℝ≥0∞ :=
  ((2 ^ droppedBits : ℝ≥0∞) / modulus) ^ (k * ringDegree)

```

[`BlindingEntropy.lean:43-45 ↗`](https://github.com/Skanislav/pq-sap/blob/main/lean/PqStealth/BlindingEntropy.lean#L43-L45)

## `blindPointMassBound_mldsa65`

```lean
theorem blindPointMassBound_mldsa65
    [SampleableType Rq]
    (P : Prims Rq (MLDSA.Bytes 32) WireBytes (Vector prims65.Power2High mldsa65.k)
      Tag Addr K mldsa65.k mldsa65.l) :
    BlindPointMassBound P (mlDsaAddressPointBeta mldsa65.k) := by
```

:::details[proof]

```lean
  sorry
```

:::

[`BlindingEntropy.lean:48-54 ↗`](https://github.com/Skanislav/pq-sap/blob/main/lean/PqStealth/BlindingEntropy.lean#L48-L54)
