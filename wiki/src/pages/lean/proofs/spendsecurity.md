# SpendSecurity

:::info[Mirrored page]
Source: [`lean/docs-proofs/spendsecurity.md`](https://github.com/Skanislav/pq-sap/blob/main/lean/docs-proofs/spendsecurity.md). Edit the source file; this copy is regenerated by `pnpm sync` in `wiki/`.
:::

## `sampleConvolution`

```lean
def sampleConvolution {X : Type} [Add X] (left right : ProbComp X) : ProbComp X := do
  let x ← left
  let y ← right
  pure (x + y)

```

[`SpendSecurity.lean:19-23 ↗`](https://github.com/Skanislav/pq-sap/blob/main/lean/PqStealth/SpendSecurity.lean#L19-L23)

## `RelatedSpendExp`

```lean
structure RelatedSpendExp (q maxAttempts : ℕ) where
  dummy : Unit := ()

noncomputable def relatedSpendAdvantage (q maxAttempts : ℕ) : ℝ := 0

```

[`SpendSecurity.lean:33-37 ↗`](https://github.com/Skanislav/pq-sap/blob/main/lean/PqStealth/SpendSecurity.lean#L33-L37)

## `CmaToNmaAssumption`

```lean
structure CmaToNmaAssumption (qS qH : ℕ) (eps pAbort zetaWide delta : ℝ) (hp : pAbort < 1) : Prop where
  eps_nonneg : 0 ≤ eps
  p_nonneg : 0 ≤ pAbort
  zeta_nonneg : 0 ≤ zetaWide
  delta_nonneg : 0 ≤ delta

noncomputable def CmaToNmaLossNN (qS qH : ℕ) (eps pAbort zetaWide delta : ℝ) (hp : pAbort < 1) : NNReal :=
  ⟨2 * qS * (qH + 1) * eps / (1 - pAbort)
     + qS * eps * (qS + 1) / (2 * (1 - pAbort) ^ 2)
     + qS * zetaWide + delta,
   by sorry⟩

```

[`SpendSecurity.lean:41-52 ↗`](https://github.com/Skanislav/pq-sap/blob/main/lean/PqStealth/SpendSecurity.lean#L41-L52)

## `UnboundedSigningAssumption`

```lean
structure UnboundedSigningAssumption (qS maxAttempts : ℕ) (pAbort : NNReal) : Prop where
  bridge : ∀ (_adv : RelatedSpendExp qS maxAttempts),
    relatedSpendAdvantage qS maxAttempts ≤
      relatedSpendAdvantage qS 0 + (TruncationLossNN qS pAbort.1 maxAttempts).1

```

[`SpendSecurity.lean:56-60 ↗`](https://github.com/Skanislav/pq-sap/blob/main/lean/PqStealth/SpendSecurity.lean#L56-L60)

## `blindedSTMSIS`

```lean
def blindedSTMSIS : ENNReal := 0
```

[`SpendSecurity.lean:62-62 ↗`](https://github.com/Skanislav/pq-sap/blob/main/lean/PqStealth/SpendSecurity.lean#L62-L62)

## `epsilonBlindExpand`

```lean
noncomputable def epsilonBlindExpand : ENNReal := 0

```

[`SpendSecurity.lean:64-65 ↗`](https://github.com/Skanislav/pq-sap/blob/main/lean/PqStealth/SpendSecurity.lean#L64-L65)
